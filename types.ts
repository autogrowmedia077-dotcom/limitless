export interface FaqItem {
  question: string;
  answer: string;
}

export interface Testimonial {
  text: string;
  author: string;
  location: string;
}

export interface ScreenTimeRisk {
  label: string;
  color: string;
  message: string;
  description: string;
}